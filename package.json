{
  "name": "taxis",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "test": "npm run test --workspace=backend",
    "typecheck": "npm run typecheck --workspace=backend"
  }
}
