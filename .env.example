# Privy app credentials (Dashboard → App settings → Basics)
PRIVY_APP_ID=
PRIVY_APP_SECRET=

# Authorization key (server-side signer / key quorum) — generate with:
#   openssl ecparam -name prime256v1 -genkey -noout -out private.pem
#   openssl ec -in private.pem -pubout -out public.pem
# Register public.pem as a Key Quorum in the Privy Dashboard (Authorization
# keys -> New key -> Register key quorum instead, threshold 1), then paste
# the resulting quorum id and the raw private key PEM contents here.
PRIVY_AUTH_KEY_QUORUM_ID=
PRIVY_AUTH_PRIVATE_KEY_PEM=

# Monad testnet
MONAD_TESTNET_RPC_URL=https://testnet-rpc.monad.xyz
MONAD_TESTNET_CHAIN_ID=10143

# AUSD (Agora) on Monad testnet — confirmed via docs.agora.finance/developer/contract-deployments
AUSD_TESTNET_ADDRESS=0xa9012a055bd4e0eDfF8Ce09f960291C09D5322dC
# AUSD testnet faucet contract (for acquiring test AUSD) — check its contract
# on the explorer for the exact mint/faucet function before scripting a call:
# https://testnet.monadscan.com/address/0xd236c18D274E54FAccC3dd9DDA4b27965a73ee6C#code
AUSD_FAUCET_ADDRESS=0xd236c18D274E54FAccC3dd9DDA4b27965a73ee6C

# Addresses used by the kill-test spike (scripts/privy-kill-test.ts)
KILL_TEST_VALID_RECIPIENT=
KILL_TEST_WRONG_RECIPIENT=
